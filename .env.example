# alpha_digest - Environment Variables
# Copy this file to .env and fill in your values:  cp .env.example .env
# Comma-separated list of tickers used when no query is provided
# Example: ALPHA_DIGEST_TICKERS=AAPL,MSFT,GOOGL
ALPHA_DIGEST_TICKERS=AAPL,MSFT,GOOGL
# Google Generative AI API key (https://makersuite.google.com/app/apikey)
GOOGLE_API_KEY=your_google_api_key

# Finnhub API key (https://finnhub.io/ — free tier)
FINNHUB_API_KEY=your_finnhub_api_key

# Optional: news articles per ticker (default 20)
# NEWS_PER_TICKER=20
# Optional: how many days back to fetch news (default 1)
# LOOKBACK_DAYS=1

# Optional: LangSmith tracing (https://smith.langchain.com/)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key
# LANGCHAIN_PROJECT=alpha_digest

# Optional: Log to a file (leave empty for console only)
# LOG_FILE=logs/agent.log
# Gmail
GMAIL_OAUTH_ENABLED=true
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=token.json
SUMMARY_EMAIL_TO=your_email@gmail.com
GMAIL_OAUTH_FLOW=local          # or "manual" for WSL

# TTS
EDGE_TTS_VOICE=en-US-AriaNeural  # optional, this is the default

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id